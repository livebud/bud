package generator

{{- if $.Imports }}

import (
	{{- range $import := $.Imports }}
	{{$import.Name}} "{{$import.Path}}"
	{{- end }}
)
{{- end }}

func New(kit buddy.Kit) (*generator.Generator, error) {
	return generator.Load(kit)
}
