package generator

{{- if $.Imports }}

import (
	{{- range $import := $.Imports }}
	{{$import.Name}} "{{$import.Path}}"
	{{- end }}
)
{{- end }}

func New(
	kit buddy.Kit,
) (*Generator, error) {
	return generator.Load(kit, generator.Map{

	})
}

type Generator = generator.Generator