package command

{{- if $.Imports }}

import (
	{{- range $import := $.Imports }}
	{{$import.Name}} "{{$import.Path}}"
	{{- end }}
)
{{- end }}

type CLI struct {
	Buddy *buddy.Driver
	Generator generator.Map
}

func (c *CLI) Parse(args ...string) error {
	println(c.Buddy.ImportPath())
	panic("Parse not implemented yet")
}
