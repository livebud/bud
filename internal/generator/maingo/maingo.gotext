package main

{{- if $.Imports }}

import (
	{{- range $import := $.Imports }}
	{{$import.Name}} "{{$import.Path}}"
	{{- end }}
)
{{- end }}

func main() {
	exitCode := command.Parse(os.Args[1:]...)
	os.Exit(exitCode)
}