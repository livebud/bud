package generator

{{- if $.Imports }}

import (
	{{- range $import := $.Imports }}
	{{$import.Name}} "{{$import.Path}}"
	{{- end }}
)
{{- end }}

func New(
	genfs genfs.FileSystem,
	module *gomod.Module,
	log log.Log,
	{{- range $generator := $.Generators }}
	{{ $generator.Camel }} *{{ $generator.Import.Name }}.Generator,
	{{- end }}
) FS {
	{{- range $generator := $.Generators }}
	genfs.{{ $generator.Type }}(`{{ $generator.Path }}`, {{ $generator.Camel }})
	{{- end }}
	return genfs
}

type FS = fs.FS
